# Local low-stock Agent SDK pipeline (run on demand, outside the Docker stack).
# Kept separate from the service requirements so it doesn't bloat the images.
#
# PREREQUISITE: the Claude Code CLI must be installed and on PATH — the Python
# Agent SDK drives it as a subprocess:
#   npm install -g @anthropic-ai/claude-code
claude-agent-sdk>=0.1.0
httpx>=0.27.0
supabase>=2.3.0
python-dotenv>=1.0.0
pydantic>=2.6.0
pydantic-settings>=2.1.0
